/**
 * Stub — returns 0 so podchannel.spec.ts triggers its built-in
 * "fill Input Excel before running" guard instead of generating test cases.
 */
export function scanRowCount(_entityType: string, _inputFile: string): number {
  return 0;
}
